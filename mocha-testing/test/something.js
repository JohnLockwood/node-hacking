var assert = require('assert');

describe('simple_test_case', function() {
  describe('basic test', function() {
    it('should run a test if there is a file by any name in the test directory', function() {
      assert(true);
    });
  });
});

